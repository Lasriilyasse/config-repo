server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/user_ms
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  secret: THIS_IS_A_VERY_LONG_AND_SECURE_SECRET_KEY_AT_LEAST_32_CHARS

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
